source "https://rubygems.org"

# Remove "jekyll" and replace it with this.
# This locks your versions to match GitHub Pages exactly.
gem "github-pages", group: :jekyll_plugins

# Keep webrick (required for Ruby 3.0+ to run the server)
gem "webrick"